\documentclass{article}
\usepackage{tikz}
\usepackage[
    % paperwidth=13.51cm,
    % paperheight=19.27cm,
    paperwidth=148mm,
    paperheight=210mm,
    margin=0cm,
    ]{geometry}
\definecolor{bgcol}{gray}{0.65}
\definecolor{maincol}{gray}{1}
\newlength\linebreadth
\setlength\linebreadth{0.5mm}
\begin{document}%
\begin{tikzpicture}[remember picture,overlay,shift={(current page.center)}]
    \begin{scope}
    \clip(-6.755,-9.635) rectangle ++(13.485,19.27);

        %% -----
        %% Option B:
        %% outer frame, inverted colours
        %% continued at the end of the file!!
        % \definecolor{maincol}{gray}{0.25}
        % \definecolor{bgcol}{gray}{1}
        %% -----
        
        %% -----
        %% Options B & C:
        %% continued at the end of the file!!
        \fill[bgcol] (-6.73,-9.61) rectangle ++(13.46,19.22);
        %% -----
    
        %% -----
        %% Option A:
        %% inner picture with sun
        % \clip(-5.625,-8.025) rectangle ++(11.25,16.05);
        % \fill[bgcol] (-5.6,-8.0) rectangle ++(11.2,16.80);
        %% -----

        \foreach \a in {0, 10,...,359}
          \draw[line width=\linebreadth, maincol] (\a:1) -- (\a:12); 
        \foreach \r in {2.33, 2.81 ,...,13}
          \draw[bgcol, line width=2mm] (0,0) circle (\r);
        \foreach \a in {5, 15,...,359}
          \draw[line width=\linebreadth,maincol] (\a:1) -- (\a:12);
          
        \fill[maincol] (0,0) circle(19mm);
        \draw[bgcol, line width=\linebreadth] (0,0) circle (1.75);
        
        \draw[line width=\linebreadth,draw=maincol] (-5.6,-8.0) rectangle ++(11.2,16.0);
        \draw[line width=\linebreadth,draw=maincol] (-6.73,-9.61) rectangle ++(13.46,19.22);
        
        %% -----
        %% Option B:
        %% outer frame, inverted colours
        % \fill[bgcol] (-5.575,-7.975) rectangle ++(11.15,15.95);
        %% -----
        
        %% -----
        %% Option C:
        %% outer frame, sun in the centre
        \fill[bgcol] (-5.575,-7.975) rectangle ++(11.15,15.95);
        \foreach \a in {0, 10,...,359}
          \draw[line width=\linebreadth,maincol] (\a:2) -- (\a:3.7); 
        \foreach \r in {2.75, 3.65,...,4}
        \foreach \r in {2.75, 3.2,...,4}
          \draw[bgcol, line width=2.5mm] (0,0) circle (\r);
        \foreach \a in {5, 15,...,359}
          \draw[line width=\linebreadth,maincol] (\a:2) -- (\a:3.5); 
        \fill[maincol] (0,0) circle(19mm);
        \draw[bgcol, line width=\linebreadth] (0,0) circle (1.75);
        %% -----
        
    \end{scope}
\end{tikzpicture}
\end{document}